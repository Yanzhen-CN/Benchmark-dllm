__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
.venv-*/
venv/

# generate/score/visualize pipeline output (model_output/score_output/visualization_output)
output/
.tmp_*/
*.png
*.gif

# HF checkpoint cache (see dllm_bench/hf_cache.py) — project-relative on
# purpose, but still not something to commit
.hf_cache/

# Official benchmark dataset cache (for example, OpenAI GSM8K test JSONL)
.dataset_cache/

# Shared package wheel cache used by model setup scripts
.pip_cache/
.pytest*/
